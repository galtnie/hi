import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
  text: {
    color: "#536179",
    textAlign: "center",
    fontWeight: "100"
  },
  title: {
    color: "black",
    fontWeight: "400"
  }
}));
